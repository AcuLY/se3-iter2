# 作业说明文档

## 任务一

- 目标：搭建一个面向特定领域的，具备自主思考和行动能力的LLM
Agent应用
- 主题：旅行规划助手 agent

基本需求点：
1. Agent需展现出规划和思考能力，而不是简单预定义的workflow；
2. Agent具备与外部世界交互的能力，根选主题，至少设计实现2个主题相关的外部工具；
3. Agent需具备自我修正能力，能够捕获系统中的异常并处理；
4. Agent需可视化内部状态流转情况（cli，web等各种形式均可）

加分项：
- Agent有机结合不止一种推理框架，如ReAct，plan-and-execute，reflexion

开发框架建议
- 现有一些Agent开发框架，如LangChain、AutoGen
- 选择框架需要考虑应用场景，不能一概而论

## 任务二

目标：搭建一个Agent应用评估平台

基本约束：前后端分离Web项目
- 前端：React + Vite + TypeScript + Tailwind CSS
- 后端：Node.js + Express + TypeScript
- 数据库：SQLite
- 评估指标：RAGAS
- 接口：REST API
- 包管理：npm

基本需求点：
- 评测任务管理能力：支持创建评测任务，支持配置评测任务（如Agent版本、数据集选择、评测方法选择等），支持评测任务增删改查和状
态管理与展示
- 评测执行与方法能力：支持多种评测方法（三种划分不独立，相互间必然有重叠）
- 按评估模式划分：面向结果（只关注输入输出）/ 面向过程（关注Agent 响应的中间过程）
- 按评估方式划分：显式指标（可以明确计算，如Token 消耗、工具调用正确率、任务成功率、响应时间等）/ 模糊指标（评价标准相对主观，一般以LLM-asa-Judge 的模式实现，例如推理质量、输出内容准确性、幻觉程度、交互体验等，可以自行设计，也可以基于已有评估框架实现）
- 按评估维度划分：效果维度（任务响应是否有效、准确、完整）/ 安全（反馈信息是否安全，是否不包含有害内容）/ 性能（用户体验是否流畅，是否出现长时间停顿等待，是否有良好的互动交流体验）
- 扩展能力：支持自定义评估指标，支持组合评估策略
- 评测分析与展示能力：支持单次评估任务可视化结果展示，支持多个评估任务间对比分析与展示

## 开发要求

**假定有六个开发人员在开发**，开发时需要将全部开发任务相对均匀地分成六份，在仓库中提交时遵循 commit convention，但要在 feat/fix 等前缀之前加上成员编号，比如：
> 成员 A - fix(frontend): 修复了按钮点击后无响应的问题